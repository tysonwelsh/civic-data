# SHIP GATE — the state that means "ready to publish"

Readiness is a STATE, not an empty TODO list. The repo is publishable when the three predicates
below hold. **Policy: an open [DEBT] item blocks publish only if it makes a published value
WRONG; incompleteness ships with its caveat row.** (Honest gaps are data — cardinal rule 1.)

## Predicate 1 — federation integrity (runnable)

```
python3 scripts/validate_entity.py --federation   # must exit 0, 44/44 in step
sqlite3 "file:gov.db?mode=ro" "PRAGMA integrity_check;"    # ok
sqlite3 "file:gov.db?mode=ro" "PRAGMA foreign_key_check;"  # no rows
```

## Predicate 2 — every known ceiling is caveat-carried IN THE DB

Every measurement ceiling asserted in a per-entity CLAUDE.md / COVERAGE.md (tally-only eras,
dissent-only naming, vote-vocabulary limits, absent layers, classification ceilings) has a
matching row in gov.db `caveat` on the CORRECT (city, dataset), and no caveat row asserts
something the data contradicts. Spot-check the known hazards:

```
-- zero-caveat entities (should return no BUILT city with a documented ceiling):
SELECT e.city FROM entity e LEFT JOIN caveat c USING(city) WHERE c.city IS NULL;
-- dissent-only/tally-only cities must be flagged where the votes actually are:
SELECT city, body, COUNT(*) n, SUM(vote='Aye') ayes FROM vote v JOIN motion m USING(motion_id)
  GROUP BY 1,2 HAVING n>30 AND ayes=0;   -- every row needs a caveat on that dataset
```

## Predicate 3 — no document asserts what the db contradicts (runnable)

```
python3 scripts/check_doc_numbers.py   # must exit 0 — asserts every headline number in
                                       # README/CLAUDE.md/gov_db_SCHEMA.md against gov.db
```

Plus the standing rule: a closure that falsifies a doc claim updates the doc in the same
session — this predicate is maintained, not re-earned. A doc rewrite that changes a checked
claim's phrasing must update check_doc_numbers.py in the same session.

## Publication mechanics (one-time, tracked as TODO.md PUBLISH GATE G1–G9)

git init private with corrected .gitignore → LICENSE/CITATION/METHODS/PRIVACY → doc pass →
search-layer fixes → consumer packaging (quickstart, gov.db.gz release asset, data
dictionary) → build hardening → the three wrong-value data fixes → declare against this file
→ publish (repo + release + Zenodo DOI + municipalsky.com link).

## Status

| date | P1 | P2 | P3 | note |
|---|---|---|---|---|
| 2026-07-31 (review) | PASS (44/44, ok, 0) | FAIL — 16 zero-caveat entities; 2 falsified caveats (utah_county, weber); 1 mis-filed (south_jordan); 1 stale (millcreek) | FAIL — README/CLAUDE.md county counts, disposition claim, cities_db_SCHEMA.md, entity counts | caveat fix in progress (G2); doc pass queued (G4) |
| 2026-07-31 (post-G2) | PASS (44/44, ok, 0; build 14:30:48) | PASS — caveat 63→88; 0 built entities uncaveated; both falsified rows rewritten; SJ PC + magna dissent-only rows verified surfacing in v_member_record_all | FAIL — G4 doc pass pending | G1 (git) + G3–G8 remain |
| 2026-07-31 (post-G4) | PASS | PASS | **PASS — check_doc_numbers.py 13/13** (README/CLAUDE/gov_db_SCHEMA reconciled; utah_county + provenance + disposition + roster claims corrected; schema doc regenerated) | G5–G8 remain, then declare |
| 2026-07-31 (post-G5/G6/G7) | PASS (auto-gate now runs on every build; 44/44 on the 15:58 build) | PASS (caveat 91, corrected texts federated) | PASS (13/13 after fts 14,713 reconciliation; marquee examples 5/5) | G8 data fixes remain, then declare + release |
| **2026-07-31 (post-G8) — ALL THREE PREDICATES PASS** | PASS (build 17:00:54, auto-gate 44/44, integrity ok, FK 0) | PASS (caveat 91; date-collision class closed — detector shows only verified-honest pairs) | PASS (check_doc_numbers 13/13; marquee 5/5) | **G1–G8 complete. Remaining = G9: owner declares, cuts release (gov.db.gz + sha256), mints Zenodo DOI, flips public, links municipalsky.com** |
| 2026-08-01 (post-DEBT-wave) | PASS (build 00:28:03, auto-gate 44/44) | PASS (caveat 91) | PASS (13/13; marquee 5/5) | DEBT queue 14→4 (12 closed, 2 new evidence-cited filed: 2021 RCV mislabel class, bluffdale motion-window — both make small published values wrong, so fix before G9 or disclose) |
| **2026-08-01 (DEBT queue EMPTY)** | PASS (build 01:08:48, auto-gate 44/44) | PASS (caveat 92 incl. the slc dual-direction row) | PASS (13/13; marquee 5/5) | **0 [DEBT] remaining. Everything open is owner-gated: G9 + 6 [GATED] bundles** |
| **2026-08-20 (post-utah-B2)** | PASS (build 2026-08-20, auto-gate **44/44**, integrity ok, FK 0) | PASS (caveat **104**; utah_county's `cf-totals-tier` row rewritten for the closed itemized layer — per-period regime, the cumulative-exact BLANK, the amendment double-count guard, and the three meanings of an empty side; only udot/uta uncaveated, which are registered-only reference entities with no data by design) | PASS (`check_doc_numbers.py` all checks; marquee 5/5) | **utah wave B2 closed: 245/245 filings, 6,513 rows, 100% geometry, 0 withheld. [DEBT] 3 → 2** (Smith 2014 filed+closed same session; rowbands DEFECT 7 and the make_snippet corpus re-check remain — **neither makes a published value wrong**, so per this file's policy neither blocks publish). **All three predicates PASS. Remaining = G9: owner declares, cuts release, mints DOI, flips public.** |
| **2026-08-20 (post-SLCo-gate)** | PASS (build 2026-08-20T18:26:52, auto-gate **44/44**, integrity ok, FK 0) | PASS (caveat **104**; salt_lake_county's `cf-county-eras` row rewritten — the itemized layer is now declared INCOMPLETE on the row itself, carrying the 245 row-less filings / 197-with-detail sizing, the dead-portal re-diagnosis, and the EasyVote donor-geography redaction rule) | PASS (`check_doc_numbers.py` 13/13; marquee 5/5) | **salt_lake_county EasyVote office-gate [DEBT] filed AND closed**: +1,228 contributions / +479 expenditures recovered from data already on disk (cf_contribution 20,930 · cf_expenditure 11,882 · cf_filing 841), 0 rows lost, cover tranche unmoved, and the 26 affected filings reconcile EXACTLY on all 52 sides against their vision-read covers. Docs corrected across 7 files incl. a falsified GOTCHAS standing rule. **[DEBT] 2 → 3** (new: index.csv school-board mislabel — catalog-only, `cf_filing` clean). SLCo's remaining gaps are now SIZED and caveat-carried rather than unknown; none makes a published value wrong, so per this file's policy none blocks publish. **Remaining = G9.** |
| **2026-08-23 (post-washington-parser)** | PASS (build 2026-08-23T14:07:56, auto-gate **44/44**, integrity ok, FK 0) | PASS (caveat **105** — washington_county's `cf-totals-tier` row rewritten for the cycle-restatement hazard, plus a NEW `cf-itemized-machine-readable` row declaring the queue derivation, the five verdict states and exactly what is still not itemized; only udot/uta uncaveated, registered-only by design) | PASS (`check_doc_numbers.py` all checks after `gov_db_SCHEMA.md`'s caveat count 104→105) | **washington machine-readable era CLOSED: 3,256 rows over 101 of 102 born-digital filings, 100% geometry, 1 side withheld, no page image read, 0 of 206 `stated_*` moved, rebuild byte-identical.** Four reading-path defects fixed at emission (one of them a latent PRIVACY leak that had not yet shipped). **[DEBT] unchanged** — nothing found here makes a published value wrong that was not corrected in the same session. **Remaining = G9.** |
| **2026-08-23 (post-county-cycle-reducer)** | PASS (build 2026-08-23T19:12:11, auto-gate **44/44**, integrity ok, FK 0) | PASS (caveat **107** — two NEW rows: `cf-cycle-tiers`, the row that prevents a naive union of the two cycle tables and carries the never-sum-a-cumulative-snapshot / never-sum-a-restating-ledger guard on every `v_cf_cycle_all` row, and `cf-cycle-county-method`, the method + its honest ceilings incl. per-county chain-closure rates and the `is_floor` semantics; `cf-coverage` REWRITTEN — its "county cycle rollups are deliberately NOT derived" sentence was falsified by this layer and was replaced in the same session) | PASS (`check_doc_numbers.py` **19/19** — 6 new assertions gate the county rollup's headline numbers, incl. the publish/gap split and the flagged-floor count) | **`cf_cycle_county` shipped: 968 candidate-cycles over all 8 counties, 618 publishing a figure, 350 honest GAP rows, 200 flagged `is_floor` lower bounds.** Safety proven, not asserted: `cf_filing` / `cf_contribution` / `cf_expenditure` / `cf_cycle` all **byte-identical** to the pre-change ordered-dump md5, `cf_cycle` still 805 rows / 29 cities / **0** county rows, and the city↔county leakage path is closed STRUCTURALLY (`cycle_totals.py::all_cities()` is `level=='city'`, `write_city()` raises on a non-city slug, and the federation loader is `e.level`-gated on two differently-named artifacts). 31-case regression suite green incl. two negative controls that must produce NO number; `validate_finance.py` 38 PASS + the 2 known non-regressions; all 8 counties' `validate_entity.py` unchanged (0 FAIL). **[DEBT] unchanged — nothing here makes a published value wrong. Remaining = G9.** |
| **2026-08-24 (post-cache+washington — PHASE B COUNTY ITEMIZATION COMPLETE except SLCo's residue)** | PASS (build 2026-08-24T00:28:36, auto-gate **44/44**, integrity ok, FK **0**) | PASS (caveat **108** — cache's `cf-totals-tier` rewritten to the closed state with the DOCUMENT-grain queue, the three meanings of an empty side and the TWO duplicate classes; washington gains a NEW `cf-itemized-vision-era` row carrying the per-filing scope split, the over-$50 anchor rule and the bundle convention) | PASS (`check_doc_numbers.py` **all checks**; 7 numbers corrected, 6 of them a `cf_cycle_county` drift the earlier SLCo W1 wave left behind — 968→**1,009** — surfaced by this wave's reducer run) | **BOTH REMAINING PHASE-B COUNTIES CLOSED.** washington **100/100** handwritten cover forms (530C+778E, 100% `pct:` geometry, **0 withheld**, 0 amounts blank for illegibility, 173/200 sides exact) — the first Phase-B county with BOTH eras closed; cache **176/176** remaining distinct documents (556C+1,119E transcribed → **756/1,466** published after the byte-duplicate fan-out, 100% geometry, **0 withheld**, 282/352 sides exact), which also closed the born-digital `cache_cfd` gaps. Queues **re-derived from the primary files**, not inherited. Calibration pre-flight **21/21 per county** (2 fresh runs, 42 specimen readings). Safety proved rather than asserted: both modules **rebuild byte-identical**, `prove_additive` shows **0 moved values** on every pre-wave row and **no `stated_*` or `amount` moved**, `validate_finance` **PASS** on both (38 PASS + 1 known non-regression across the repo), 93 family tests green, and **`cf_cycle_county` published totals are UNCHANGED in both counties** (0 figure changes; only the itemized cross-check improved — washington 0→15 cycles cross-checked, cache 5→50). Two shared-file changes, both additive and documented: `common.parse_vision_amount` (the handwritten-cell reader, which refuses the malformed decimals the calibration suite requires to stay blank) and a WIDENING of `validate_finance`'s candidate/year check to admit `cycle_year`. **[DEBT] unchanged — nothing here makes a published value wrong that was not corrected in the same session** (36 OCR-garbled washington candidate names WERE corrected, in the layer that owns them). **Remaining = G9.** |
| **2026-09-01 (post-SLCo W2 — COUNTY ITEMIZATION PROGRAMME COMPLETE)** | PASS (build 2026-09-01T23:59:22, auto-gate **44/44**, integrity ok, FK **0**) | PASS (caveat **108** — salt_lake_county's `cf-county-eras` row REWRITTEN: its opening no longer says the itemized layer is incomplete, its EasyVote section is now the CLOSED state with the derived-queue arithmetic, the per-side ledger, the kimi-k3 provenance string, the Office-Sought-not-Office classification rule, the Wilson redaction floor, the donor_occupation correction and the negative-expenditure abs() rule) | PASS (`check_doc_numbers.py` **all checks** after 6 numbers corrected — `cf_cycle_county` 1,009→**1,008** / 620→**657** publishing / 389→**351** gaps / 201→**222** floors, plus the stale `cf_contribution`/`cf_expenditure`/`cf_filing` headline trio in gov_db_SCHEMA.md and the SLCo county filing count in README) | **SALT LAKE COUNTY'S EASYVOTE RESIDUE CLOSED — and it is the repo's FIRST EXTERNAL-AGENT TRANSCRIPTION.** Transcribed 2026-08-24 by Kimi K3 under `W2_HANDOFF.md` at the handoff's minimum bar; this session ran the deterministic tail. Derived queue **240 = 238 transcribed + 2 school-board out of scope**, remaining **0**; **18,240 rows** (11,852C + 6,388E), **100% `pct:` geometry**, **ZERO withheld**, 141 new covers. Per side (480): 359 exact · 33 delta-with-cause · 82 `none` · 2 unknown · 4 out-of-scope. **Trust was verified, not assumed**: gates re-run independently (`validate_finance` PASS, `screen_records` 240/240 with 0 missing, `checkpoint` OK, `vision_coverage` remaining 0), **rebuild byte-identical**, frozen pre-wave blocks proved field-for-field (**0 `stated_*` moved**), and **4 filings re-read at the page** across the ledger tiers — every published row matched the document, including two column-clipped donor strings correctly kept as visible glyphs rather than completed, and the out-of-scope ruling re-proved at the cover. Reducer regenerated: **37 candidate-cycles GAP → PUBLISH**, **0 published figure moved**, **0 rows changed in the other seven counties**. 38 CF modules PASS + the 2 known non-regressions; 93 family tests + the 31-case reducer suite green. **[DEBT] +1 (evidence-cited): 9 malformed `geometry` pointers** — provenance pointers only, no money value affected. **Remaining = G9.** |
