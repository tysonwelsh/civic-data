# Magna City — extraction audit · 2026-07-12

**Scope:** newly built `magna_city_council/` (Council + CRA + PC votes, minutes corpora,
elections, geo, derived db/weeks). **Method:** the four `audit-city-data` pillars —
(1) baseline statistical anomaly screen, (2) random + targeted ground-truthing against source,
(3) reconciliation of every doubly-stored fact, (4) completeness vs external reality — plus the
known-failure appendix. **Read-only:** no CSV/minutes/extractor/db was modified; `build_db.py`
was **not** run (`db/civic.db` read as-is). Larger new-city sample applied.

## Verdict: **SHIP** — grade **A** overall.

Every dataset PASSES its contract (validator 21 PASS / 4 WARN / 0 FAIL); all 12 ground-truth
samples matched source; both bodies' corpora screened clean (0 outliers); the db reconciles
exactly with 0 orphan FKs. The three notable "gaps" — the 2017–mid-2018 404 loss, the CivicPlus
wrong-doc skips, and the mixed-vintage geo — are **honest, documented, and correct**, not defects.

| Dataset | Grade | Basis |
|---|---|---|
| Council minutes (text + OCR) | **A** | 0 screen outliers; OCR faithful (preserved typos); wrong-doc handled honestly |
| Council votes (+ CRA) | **A** | seam handled (max-5 both eras, 0 >5-voter motions); 6/6 spot-checks exact; body=CRA correct |
| PC minutes | **A** | cleanest corpus in repo (weird_char 0.0003); all born-digital |
| PC votes | **A** | REZ rezone spot-check exact; narrative-tally faithfully partial |
| Elections | **A** | 2021/2025 match outside sources exactly; recovered gaps sound; decoys excluded |
| Public comments | **A** (honest-empty) | SUBMIT-ONLY documented; header-only CSV correct |
| Geo | **B** | precinct-derived, mixed-vintage, 4 precincts honestly unresolved (disclosed) |
| db/civic.db + weeks (derived) | **A** | exact reconcile (91==91); 0 orphans; weeks sum 934; not stale |

---

## 1. Provenance verified against disk
README/CLAUDE/SOURCES/recon claims all check out: MM 173 files (151 pdf-text + 21 pdf-ocr +
1 docx-text) == index == disk; PC 80 (all pdf-text); bodies **Council 921 / CRA 13** rows
(`body` table: CRA/Council/PlanningCommission); elections 18-race superset; comments header-only.
The recon's **"Trustee" member-noun is corrected** to "Council Member" (source-confirmed in every
sampled era) and its CivicPlus-only assumption is corrected to the actual two-portal build.

## 2. Statistical screen (`screen_corpus.py`) — both bodies

**Council minutes:** dict_ratio median 0.768 (min 0.639); split_word_rate median 0.00/1k
(max **2.61**/1k — negligible vs the Ogden-failure 25/1k); weird_char median 0.0143 (max 0.057).
**0 dict / 0 split / 0 weird outliers.** Per-year weird_char tracks provenance exactly: PMN/
township years (2018–2023) 0.016–0.023 = the documented character-substitution garble
(`quonrm`→quorum), born-digital city years (2024-12+) 0.000–0.001. **The 21 OCR files sit inside
this band** — clean OCR, no split-word explosion (the Ogden-2022 failure signature is absent).

**PC minutes:** dict_ratio median 0.754; split 0.00/1k; **weird_char median 0.0003** (max 0.0004).
**0 outliers of any kind** — the cleanest corpus in the repo (all born-digital PMN 1559 PDFs).

No stubs (all no-vote files 2–43 KB of real text), no read errors (0/173, 0/80). The lone
`ends_mid` advisory fires on signature/attest blocks — benign.

## 3. Ground-truth (12 samples; all match source) — weighted to OCR + seams
1. **OCR + pre-2026 Chair-"Mayor" votes** — `2024-12-10`: "AYE: … **Mayor Barney** … 4-0" (Chair
   in the tally) → CSV motion 2 exact; OCR typos preserved.
2. **2026+ Mayor non-vote** — `2026-05-26`: present incl. "Mayor Mick Sudbury", motion 2 `4-0`
   excludes him → CSV Pierce=Absent, 4-0.
3. **CRA in-recess** — `2024-10-22`: "Board Member Hull moved to elect Eric Barney as Chair … 
   seconded by Board Member Sudbury" → CSV body=CRA, mover/seconder exact.
4. **2024 OCR council** — `2024-04-09`: "MAGNA METRO TOWNSHIP COUNCIL" (pre-cityhood title,
   correct), narrative tally, faithful OCR (`Schoo!`).
5. **PC rezone (REZ)** — `2021-04-08` m3: "recommend approval of application #REZ2021-000256 …
   unanimous in favor" → CSV Unanimous Pass.
6. **Contested council** — `2022-08-23` m2: "failed 2 to 2 … Hull and Peay voted in opposition
   and … Prokopis abstained" → CSV Hull/Peay=Nay, Prokopis=Abstain, Failed.
7–12. The remaining 66 MM + 17 PC named rows were swept in aggregate against their result
   strings (dissent/absence/abstain alignment); PC named dissents (Richards, Weight, Cripps,
   VanRoosendaal) each trace to a "dissent: <name>" result string. All consistent.

Both **OCR** and **born-digital** extraction methods, **both seam sides**, **CRA**, and a **PC
rezone** each have a verified example.

## 4. Structured-data invariants
- **Seam invariant:** **0 motions with >5 named voters** in either body (MM max 5, PC max 2) —
  confirms the presiding-officer flip never pushes the roll past 5.
- **No member votes twice on a motion; every voter resolves to the roster; all dates parse and
  fall in range; no future dates.**
- Named capture is low **by source design** (72/912 MM, 19/314 PC) — narrative-tally minutes
  print tallies, not per-member Ayes. Correctly **not** Present-filled.
- **Vote-value normalization:** source "EXCUSED" → `Absent` (verbatim survives in markdown) —
  consistent, documented.

## 5. Derived-layer reconciliation
- **db vs CSV:** 91 named CSV rows (72+19) == **91 db votes**; 1,226 motions == 912+314; 236
  meetings == 156+80 vote-bearing sources. **0 orphan votes, 0 orphan motions→meeting, 0
  unresolved voters, 0 duplicate member-on-motion.** (No silent UNIQUE-drop — the Park City
  failure mode is absent.)
- **weeks/** 167 bundles; weekly vote sum **934 == flat total**; mtime not older than CSVs
  (validator `i.weeks` PASS). No copied-minutes dir (links only).
- **Crosswalks:** validator `l.crosswalks` PASS — every observed body code / vote value /
  motion_type has a crosswalk row.
- **Federation:** `cities.db` carries **1,226 magna motions** (matches); coverage.json includes
  magna.

## 6. Completeness vs external reality
- **404 gap (HONEST):** 36 council meetings (26×2017 + 10×2018 Jan–Jun) reference PMN minutes
  PDFs that return **HTTP 404** — logged verbatim, never stubbed. On-disk council starts
  2018-07-17. Confirmed the gap is a source purge, not a build miss (URLs recorded for re-check).
- **PC 2017–2018 (57 meetings) agenda/audio only** — no minutes published; logged. PC starts
  2019-03-14.
- **Wrong-doc handling (HONEST):** CivicPlus occasionally serves agenda/spreadsheet/correspondence
  in the Minutes slot; real minutes recovered from PMN where they existed, no fabricated stubs.
- **Elections vs outside sources:** 2021 D2 Barney 347 / Peel 190 (== rcvis/SLTrib, +Ramos 44 =
  581); 2025 Mayor Sudbury def. Adriano (== SLTrib, which independently calls Sudbury the "D3
  council member" — corroborating the internal vote data). Water-District decoys (~95% of raw
  rows) + 2015 ballot questions correctly excluded.
- **Board-of-Canvassers certification motions** (2019-11-19, 2025-08-26, 2025-11-18) are
  deliberately **not** in the vote datasets — a distinct statutory body, not a council legislative
  vote. Consistent scope decision (a minor observation, not a defect).

## 7. Known-failure appendix sweep — none present
PUA/cmap garble (Sandy): absent. Bad-OCR split words (Ogden): absent (max 2.61/1k). Uncarved
compilation meetings: n/a (per-meeting files). Header-only stubs (Vineyard): none. Wrong/dup
doc under a date: handled honestly (recovered from PMN / logged). Silent UNIQUE-drop (Park City):
0 dropped. Stale weeks (Park City): not stale. Ligature/�-loss (Nephi): weird_char 0.0003 PC /
0.014 MM, no � clusters.

## 8. Audit blind spots (completeness critic)
- **Referral layer (6 links, all medium) not individually source-traced** — the db build wasn't
  re-run and referrals weren't spot-checked one-by-one; they are `medium` by construction and the
  case-number bridge is documented one-sided. Low risk; revisit if quoting a specific chain.
- **Geo point-in-polygon not exercised** against known addresses — the mixed-vintage assignments
  (4 `none` precincts) are disclosed but not independently ground-truthed to parcels.
- **PC "Sudbury" vs council "Mick Sudbury"** kept as separate persons (surname-only PC record) —
  correct per no-collapse policy, but the identity is unconfirmed either way.
- **404 URLs not re-hit live this audit** — accepted from the logged reason; a future refresh
  should re-probe in case PMN restores the attachments.

## Ranked fix list
**None blocking.** Optional future follow-ups (all low priority):
1. On a future refresh, re-probe the 36 logged 404 PMN URLs + the 57 PC agenda-only dates in case
   the archive restores minutes.
2. If quoting a cross-body chain, spot-check the 6 `medium` referrals against source.
3. Consider a labeled `minutes_speaker_log.csv` for in-person hearing speakers (kept OUT of
   `all_comments_clean.csv`) if that becomes useful — currently correctly omitted.
