# roster_overrides.csv — hand-editable correction layer for the Magna council roster.
#
# This is the repo's documented override convention (see civic-data/CLAUDE.md cardinal
# rule 2: city-faithful automated output is never overwritten in place — corrections
# live here, alongside).  build_roster.py applies this file LAST and it WINS TIES.
#
# Use it to record anything the automated reconciliation (elections + vote-bounds +
# minutes events) cannot derive, e.g.:
#   - an announced resignation that was never formally voted,
#   - a death in office,
#   - a source conflict where you have adjudicated the correct value,
#   - a corrected appointment date once a minutes source is found.
#
# Match key: (seat_id, person_key, start_date) must equal an existing tenure to patch
# it; supplying a NEW (seat_id, person_key, start_date) triple inserts a tenure the
# reconciliation missed.  Any non-empty column overrides the derived value; blanks are
# ignored.  Always fill `reason` with the human justification + source.
#
# May legitimately contain zero data rows (as it does now — every current tenure is
# derived from a cited source with no conflict).
seat_id,person_key,start_date,person_name,end_date,start_event,end_event,election_year,confidence,note,reason
