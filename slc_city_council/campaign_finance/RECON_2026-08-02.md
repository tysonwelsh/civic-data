# SLC campaign finance — adversarial channel hunt, 2026-08-02

**Purpose.** The 2026-07-05 determination in `AVAILABILITY.md` — *"the dotnet.slcgov.com
portal is the one real source; no PDFs exist anywhere; the state tree doesn't carry SLC
city candidates"* — was a single-session hypothesis. This addendum re-tests it
channel-by-channel. Precedent: the juab "no public posting" determination was falsified by
a second look.

**Result: the negative was PARTLY WRONG.** Two of its three claims are falsified.

---

## Headline findings

1. **A PDF era EXISTS and was recovered.** Salt Lake City's Recorder published
   per-candidate campaign-finance disclosure PDFs at
   `slcgov.com/recorder/fin_disc/pdfs/<Candidate_Name>.pdf`, indexed by
   `recorder/fin_disc/feb_fin_disc.htm` ("**February 15th 2003 Candidate Financial
   Disclosures**"). These are **full filings** — summary, balance summary, **itemized
   contributions with donor names/amounts/dates, and itemized expenditures**. The index
   lists **11 candidates**; **8 recovered** (see below). This falsifies
   *"No PDF/document filings anywhere (portal is data-only)"*.

2. **A PREDECESSOR SYSTEM existed, 2003–2019.** Before the Angular SPA there was the
   "Salt Lake City Corporation — Candidate Finance Reporting System", an ASP.NET WebForms
   app at `dotnet.slcgov.com/ManagementServices/CandidateReporting/` (also served from the
   now-DNS-dead `apps1.slcgov.com`). Its `candidate_search.aspx` year dropdown enumerates
   election years **2003, 2005, 2007, 2009, 2011, 2013, 2015, 2017, 2019**. This falsifies
   *"No pre-portal (2007–2017) filings online"* as a statement about what the city ever
   published — those cycles WERE online, in a system now broken.

3. **The live 503 "maintenance" page is NOT empty — it carries live data.** The 35,503-byte
   page served for every dynamic request under `CampaignFinance_Public` embeds a
   **"Campaign Information" table: 38 candidates × (name, election year, office, balance)**,
   "Balance as of April 2026", for the open 2027 and 2029 SLC campaigns. Captured. The
   July session treated this response as a pure outage page.

4. **The outage is APP-SPECIFIC, not a dead server.** The sibling candidate-filing app
   `Attorneys/CampaignFinance_Candidate/api/CampaignFinanceAPI/*` answers **HTTP 401**
   (`{"Message":"Authorization has been denied for this request."}`) with live ASP.NET
   headers, while the public app's identical API path returns **503**. The database is
   demonstrably alive (it renders the balance table on the 503 page). This reframes the
   blocker: not "the data is gone" but "the public read surface is switched off".

---

## Per-channel probe log

| # | Channel | Probe | Outcome |
|---|---|---|---|
| 1 | **Wayback captures of the API itself** | CDX `dotnet.slcgov.com/Attorneys/CampaignFinance_Public*` (53 rows); CDX of the whole `slcgov.com` **domain** (86,666 unique URLs) grepped for `CampaignFinanceAPI` | **NEGATIVE, earned.** 0 API captures. Only the SPA shell + vendored `node_modules`/CSS/fonts. Wayback cannot substitute for the live API. |
| 1b | **SPA JS endpoint enumeration** | Fetched live `app/Services/campaign-finance.service.js` (public, 14,457 B) and the candidate app's (23,622 B) | **POSITIVE (method).** Recovered **10 endpoints the repo had not documented**: `GetComplianceByElection`, `GetComplianceByElectionCandidate`, `GetContributionCountByElectionCandidate`, `GetContributionListByElectionCandidate`, `GetContributionSummaryByContributor`, `GetContributorById`, `GetElectionReportNames`, `GetLookupById`, `GetLookupListByCategoryKey`, `GetExpenditureCount/ListByElectionCandidate`. Base is `UserInfo.urlPrefix + "api/CampaignFinanceAPI/"`. The SPA bundles `angular2-csv` ⇒ the public UI has a **CSV export**. |
| 2 | **Predecessor systems (pre-2018)** | CDX of the whole `dotnet.slcgov.com` host (2,124 rows); enumerated `ManagementServices/CandidateReporting*` (146 captures 2010–2025) | **POSITIVE.** Predecessor app found; year dropdown = 2003…2019. Pages: `default.aspx`, `candidate_search.aspx`, `contributor_search.aspx`, `login_page.aspx`, `email.aspx`, `get_password.aspx`. **Result pages are POST-only WebForms postbacks — none archived** (0 rows with query strings). Live: **HTTP 500** on every `.aspx` (IIS `web.config` "allowDefinition='MachineToApplication'" error — the virtual dir is no longer registered as an IIS application); static assets under it still serve 200. |
| 2b | **Alternate predecessor host** | `apps1.slcgov.com/ManagementServices/CandidateReporting/` (found linked from the archived `recorder/zelections.htm`) | **NEGATIVE.** `apps1` fails DNS. `apps.slcgov.com` resolves (Cloudflare) but returns **522** (origin down). CDX holds 7 apps1 pages (2006) — same POST-only forms, no results. |
| 2c | **Recorder PDF era** | CDX `slcgov.com/recorder/*` + `/Recorder/*` (2,946 captures) | **POSITIVE — the main find.** `recorder/fin_disc/` holds the 2003 candidate disclosures. Also (forms/《not filings》): `2-46_Campaign_Financing_Disclosure.pdf`, `Personal_Committee_Mayor/Council.pdf`, `Limit_Campaign.pdf`, `Decline_Limit.pdf`, `Termination_Statement.pdf`, `Political_Committee.pdf`, `declared.pdf`, `2005_Candidates.pdf`, `2011candidates.pdf`, `2003_Candidate_Guide.pdf`. |
| 6 | **PMN (Utah Public Notice)** | JSON POST `/pmn/searchresult.html` with `X-CSRF-TOKEN` from `search.html` meta, full param set, `sortColumn`/`sortOrder` empty | **NEGATIVE, earned.** `entityName='Salt Lake City' + agenda='campaign finance'` ⇒ *"No results found."* The query is demonstrably live: the same search without the entity filter returns real rows (Hyrum, Ogden, Rich County, Wellsville campaign-finance notices). SLC files no CF material to PMN. |
| 8 | **Live host, adversarially** | Path/casing/dup-slash/trailing-slash/query variants of the API; `http` vs `https`; alternate app roots (`/Attorneys/CampaignFinanceAPI/`, `/Attorneys/CampaignFinance_API/`, `/api/...`); subdomain DNS sweep (`apps1`, `apps`, `dotnet2`, `dotnettest`, `dev`); static-data sweep (`data`, `reports`, `exports`, `app/data.json`, `elections.json`, `web.config`) | **NEGATIVE for a public read path**, but see finding 4. Every dynamic path under `CampaignFinance_Public` returns the same 503 (`retry-after: 3600`); unknown paths 404; `Candidate` app 401. No static data cache exists under the app. |
| 8b | **Candidate-app API** | Read-only GETs of the public-equivalent endpoints | **AUTH-WALLED.** All return 401 or 404. **No authentication bypass was attempted** — enumeration stopped at the auth boundary. |
| — | **archive.today** | `archive.ph/timemap/link/` for both portals | **BLOCKED** (captcha / "complete the security check"). Not a negative — untested. Worth a browser retry: archive.today renders JS, so a capture of the SPA would carry rendered data. |

Channels 3 (slcdocs.com), 4 (state tree), 5 (county / EasyVote) and 7 (third-party mirrors)
were run in parallel; see "Parallel-channel results" below.

---

## What was acquired

### `raw/recorder_2003/` — the 2003 PDF filings (NEW)

Source: Wayback captures of `slcgov.com` / `www.slcgov.com` `recorder/fin_disc/pdfs/`.
Exact per-file Wayback timestamps + sha256 + byte counts in `raw/_fetch_log.jsonl`.
Text sidecars rendered to `text/recorder_2003/` (`pdftotext -layout`).

| Candidate | Office sought | Submit date | Contributions (period) | Expenditures (period) | Ending balance |
|---|---|---|---|---|---|
| Ross C. "Rocky" Anderson | Mayor | 2/14/2003 | 66,174.11 | 80,765.11 | 63,240.65 |
| Frank R. Pignanelli | Mayor | 2/18/2003 | 100,275.69 | 2,968.15 | 97,307.54 |
| David Buhler | City Council #6 | 2/14/03 | 2,510.00 | 1,601.45 | 5,745.31 |
| Carlton J. Christensen | City Council District #1 | 2/18/2003 | 2.01 | 719.82 | 241.48 |
| Van Turner | City Council #2 | 2/18/03 | 0.00 | 0.00 | 0.00 |
| Carol A. Goode | City Council District #4 | 2/18/2003 | 0.00 | 42.24 | −42.24 |
| Russell Farrell | City Council District #4 | 2/18/03 | 50.00 | 0.00 | 50.00 |
| Jill Remington Love | City Council District #5 | 2/18/03 | 0.00 | 550.00 | 2,093.85 |

Values are **verbatim from the filings**, which carry itemized donor rows (name, amount,
date, Ind/Bus/Pac descriptor) and itemized expenditure rows (payee, amount, date, purpose).

**Structured-build sizing** (approximate line counts from the `pdftotext -layout` sidecars —
indicative, not a parse): Ross Anderson ~244 contribution / ~133 expenditure rows ·
Pignanelli ~62 / ~8 · Buhler ~50 / ~10 · Christensen ~2 / ~10 · Goode ~1 / ~3 ·
Love ~1 / ~4 · Farrell ~2 / ~1 · Turner ~1 / ~1. Order **~360 contribution + ~170
expenditure rows** across the tranche. All 8 are **born-digital text PDFs** generated by the
reporting system — machine-readable, no OCR needed, consistent field layout
(`Name / Office Seeking / Submit Date / Is this an amendment? / SUMMARY / BALANCE SUMMARY /
ITEMIZED CONTRIBUTIONS / ITEMIZED EXPENDITURES`).

**Honest gaps in this tranche (3 of 11 index entries):**
- `David_Spatafore.pdf`, `J_Michael_Clara.pdf` — captured by Wayback (200,
  `application/pdf`, ts `20031019023620` / `20031019014713`) but the Archive is currently
  serving a **donation interstitial** instead of the object. Transient, not an absence;
  **retry**.
- `Dale_Lambert.pdf` — linked from the index but **never captured by Wayback** (0 CDX rows
  on either host). Genuinely unrecovered.

### `raw/portal_snapshot/` — the live 503 page (NEW)

`portal_503_under_construction_2026-08-02.html` (35,503 B). Carries the embedded
**38-row candidate/office/balance table, "Balance as of April 2026"**, for the open
**2027** (Council D2, D4, D6, Mayor) and **2029** (Council D1, D3, D5, D7) SLC campaigns —
including Mendenhall $22,956.92, Puy $21,163.19, Carlsen $15,775.63, Petro-Eschler
$12,314.56. Balances only; no itemization. This is a **point-in-time snapshot**, not a
filing, and should be labelled as such if it is ever structured.

---

## Corrected source determination

| 2026-07-05 claim | Status after 2026-08-02 |
|---|---|
| "SLC self-hosts; the dotnet portal is the one real source" | **Still true for 2019+ itemized data**, but incomplete: SLC also ran a 2003–2019 predecessor system and, earlier, published PDFs. |
| "No PDF/document filings anywhere (portal is data-only)" | **FALSE.** 8 recovered 2003 filings prove a document era. The claim was true only of the *current* SPA. |
| "No pre-portal (2007–2017) filings online" | **FALSE as stated.** Those cycles were online in `CandidateReporting` (year dropdown 2003–2019). What is true is that **no capture of their result pages survives** — the app was POST-only and is now HTTP 500. |
| "SLC files nothing to disclosures.utah.gov" | See parallel-channel results. |
| "Live API genuinely down" | **True, and now better characterised**: app-specific 503 while the sibling candidate app is up (401) and the DB is live (it renders the balance table). |

**Bottom line for the coordinator:** the itemized 2019–2025 layer still requires the live
public API (or the city). But the negative "nothing exists anywhere" is dead: a 2003
document tranche is in hand, and the 2005–2019 cycles are known to have been published in
a system whose files still sit on the city's IIS box (`D:\IISRoot\dotnet.slcgov.com\
managementservices\candidatereporting\`, per the live 500 error) — a GRAMA-shaped ask, not
a scraping problem.

---

## Parallel-channel results (3, 4, 5, 7)

### Channel 3 — slcdocs.com / slcgov.com / slc.gov document hosts

| Probe | Outcome |
|---|---|
| CDX whole `slcdocs.com` (12,986 unique URLs; 287 under `/recorder/`) | **NEGATIVE for filings.** Every campaign-finance PDF there is a **blank form** — verified by *rendering* each one, not by filename (`PaperCampaignFinanceReportForm.pdf`, `2015_pfs.pdf`, `PAC Finance Statement.pdf`, `financestatement2013.pdf`, `PCC_Forms.pdf`). |
| CDX legacy `slcgov.com` (69,642 URLs; `/recorder/` prefix fully enumerated, 982 URLs) | **POSITIVE — the 2003 tranche** (same find as channel 2c, independently reached). **Scope limit, earned:** there is **no second `fin_disc` index and no other candidate-named directory** on either host. |
| CDX current `www.slc.gov` (126,465 URLs; 33,034 `wp-content/uploads`) + the **WordPress REST media API** on the attorney subsite | **NEGATIVE.** `contribution` → 0, `expenditure` → 0, `campaign` → 1 (a PNG). |
| Search engines (3 passes) | **NEGATIVE.** Only ever surfaces the blank form and the dead portal. |
| Council packets / PrimeGov / repo FTS | **NEGATIVE.** `fts_packet` has **0** SLC rows; `packets/index.csv` (583 rows) 0 matches; the 8 `fts_minutes` SLC hits are all **legislative** (§2.46 amendments 2021/2024/2025) with no filings attached. |
| Live `slc.gov/attorney/recorder/conflict-of-interest-disclosures/` | **PARTIAL — different instrument.** Real documents, but *officeholder conflict-of-interest* disclosures, **not campaign finance**. (`slcdocs.com/recorder/EO_Disclosures/<Name>_2025Disclosure.pdf`; directory listing 403, files fetch by exact name; slc.gov links them with **backslashes**.) |

**Why the era ended:** the archived 2011 recorder page (`/recorder/pages/financial.htm`,
ts `20110316151953`) states the rule — file **electronically via the Campaign Finance
System**, or hand/fax/email paper to the Recorder. **From 2005 on, nothing was posted.**

Two supplementary tranches were acquired from this channel (both **CF-adjacent, not
filings** — labelled as such):
- `raw/recorder_limitations/` — **12 of 15** archived per-candidate "Public Notice"
  declarations to voluntarily limit contributions/expenditures, cycles 2003/2005/2007
  (Becker, Christensen, Comstock, Doughty, Garrott, Goode, Martin, McConkie, McDonough,
  Muscheck, Reddick, Wilson). Scanned image PDFs — **no text layer**; would need
  vision/OCR. 3 blocked by the IA interstitial (Buhler, Hughes, SaxtonNotice) — retry.
- `raw/recorder_open_committees/` — the **live** `slcdocs.com/recorder/Open Committee's.pdf`:
  22 open Personal Campaign Committees as of **2019-05-03** (name, office, voluntarily-limited
  Y/N), covering Mayor + Council D1–D7 for the 2019 cycle. Machine-readable text; sidecar
  rendered.

### Channel 4 — disclosures.utah.gov / municipal.utah.gov  →  **CONFIRMED NEGATIVE, earned**

The prior "no" survives, and is now evidence-backed rather than assumed.

- Recursive crawl of `/Municipal/salt lake`: **90 folders, 667 file links; 667/667
  downloaded**; 407 image-only files **OCR'd** (tesseract, 200 dpi).
- **Classified by the office line inside each form**, per the GOTCHAS label-lies rule —
  in-form jurisdiction census: Cottonwood Heights 69 · Midvale 69 · Herriman 60 ·
  West Jordan 56 · Murray 47 · Riverton 43 · Taylorsville 36 · Millcreek 35 · Bluffdale 23 ·
  South Jordan 23 · Holladay 16 · Draper 15 · West Bountiful 14 · South Salt Lake 9 ·
  **Salt Lake City 0**. The 9 apparent "SLC" hits are all **South** Salt Lake.
- **The residence-town trap was hunted specifically.** The two flat folders with no city
  subfolders — `2009 Primary` (61 files) and `2011 Municipal` (60), both SLC election
  years and the highest-risk hiding places — were read **file by file**: zero SLC.
  SLC-specific marker sweep over all 667: `Meeker|Trishman` (SLC recorders) 0 ·
  `Salt Lake City Corp` 0 · `451 S State` 0 · `slcgov|slc.gov` 0 · SLC+Council/District 0.
- The one file sitting in an SLC-named folder is **not an SLC filing**:
  `salt lake_2013 Municipal_Salt Lake City / Financial Campaign Report - Sweat.pdf` —
  candidate Corey Dee Sweat, addressed **"TO: Heidi Voordeckers – City Recorder/Auditor"**
  (**West Bountiful**'s recorder), "District Office: NA" (SLC seats are districted); he
  appears in no SLCo canvass 2007–2025. Textbook label-lie: the folder records his
  **mailing city**.
- **No pre-2009 folder exists at all** — 2003/2005/2007 are absent from the state system.
  (Existence discriminator validated: nonexistent → a 63-byte
  `Path: \\172.16.120.206\Municipal\salt lake\2007 does not exist.`; exists-but-empty →
  an ~11.3 kB page. The negatives are distinguishable, not guessed.)
- State search UI (working endpoint `/Search/PublicSearch/SearchEntities?term=`, control
  passed): 27 SLC names queried. Every SLC figure present appears **only as a state
  legislative registration**. One SLC-adjacent positive — the *Erin J Mendenhall Leadership
  Fund* PAC (entity 1415339) — is a **nil record**: "No Reports Have Been Filed for 2019";
  its Final Report affirms no contributions and no expenditures.
- **The state disclaims SLC in its own tree:** `salt lake_2023` links "Salt Lake City →
  dotnet.slcgov.com/…/CampaignFinance_Public"; `salt lake_2025` links
  "Salt Lake City (Updated) → slc.gov/attorney/list-of-declared-candidates/".
- Statutory reason, from the Lt. Governor's own site: **Utah Code §10-3-208** — municipal
  candidates file with their **municipal clerk/recorder**.

*Honest residual:* 152 of 667 carry no explicit in-form jurisdiction line (mostly 2013 West
Valley, 2023 Brighton, OCR-degraded 2009/2011). Their non-SLC classification rests on folder
label + filename + the zero-hit result on all five SLC markers, not on a read office line.
That is the ceiling of this sweep.

### Channel 5 — Salt Lake County / EasyVote  →  **CONFIRMED NEGATIVE, earned**

| Probe | Outcome |
|---|---|
| EasyVote token + `/filer/offices/{customerId}` | 200; **64 offices** — 10 county offices + County Council seats, 5 metro townships (Copperton, Emigration Canyon, Kearns, Magna, White City), school boards (incl. **Salt Lake City School Board D1–7**). **No SLC Mayor. No SLC City Council.** |
| `/filer/documentsearch/{customerId}` | 200, 354,511 B — **228 filers / 970 documents**; no SLC municipal office in the distribution. |
| SLC politicians present? | Two, both **non-municipal**: Ana Valdemoros → **SL County Council D1**; Andrew Johnston → **SLC School Board D5**. |
| Tenant enumeration (`customer`, `customers`, `customer/all`, `filer/entities`, `election(s)`, …) | All 404. |
| DNS: 18 SLC-shaped EasyVote slugs | **All NXDOMAIN** (controls `saltlakecountyut`, `sandycityut` resolve). No SLC tenant. |
| `saltlakecounty.gov/clerk/elections/financial-disclosures/` | 200 — exactly four categories (EasyVote 2022+, County Offices, Local School Board, Metro Township Councils) + disclosures.utah.gov for state offices. **No city/municipal category**; cites §10-3-208. |

The metro-township precedent does **not** extend to SLC: the county posts township sets
because it *administers* those filings; SLC's stay with SLC's recorder.

### Channel 7 — third-party mirrors  →  **NEGATIVE for data; prose only**

| Probe | Outcome |
|---|---|
| **FollowTheMoney / NIMP** — site search, `show-me`/`search-results` UI, `api.followthemoney.org`, their own local-coverage report | **NEGATIVE.** Their local program is Knight Foundation's 26 communities; **no Utah jurisdiction**. |
| **Ballotpedia** — 2023 SLC municipal-elections page (via Wayback `20240227113822`; live fetch is 202-blocked) | **NEGATIVE.** Zero occurrences of "campaign finance", zero dollar figures. |
| **GitHub / data repos** — repo search ×4, authenticated `gh` **code** search ×4, data.world / Kaggle / Dataverse / ICPSR | **NEGATIVE.** No scraper of `CampaignFinanceAPI`, no cached JSON. (This was the jackpot hypothesis — a third-party scraper's cache would have held the now-503 data. It does not exist.) |
| **Transparency USA / OpenSecrets / MapLight / LittleSis** | **NEGATIVE.** MapLight is now a *vendor* of disclosure software, not a data holder; OpenSecrets is federal+state. |
| **The Accountability Project** (IRW) — UT datasets 397/340 | **NEGATIVE for SLC**, but a real bulk mirror: Utah contributions 2000–2023 (**1,382,118 records**) + expenditures 2000–2020 (844,519), publisher = **Utah Lt. Governor's Office** — so it inherits the confirmed absence of SLC city filings. |
| **Candidate campaign sites via Wayback** — CDX of 13 candidate domains, PDF-filtered | **NEGATIVE.** Only `erinmendenhall.com` has PDFs (17) — all policy plans and yard signs. |
| **Utah watchdogs** — Alliance for a Better Utah, LWV Salt Lake | **NEGATIVE.** Better Utah publishes the *law* (limits), not data. |
| **DocumentCloud / MuckRock** | **NEGATIVE but WEAK** — DC's anonymous API was not phrase-filtering (identical top results regardless of query), so this channel is **not fully exhausted**; it needs an account token to query properly. |
| **News** | **PARTIAL POSITIVE — prose only, not retrievable.** |

**What journalism holds** (2023 and 2025 cycles only; no link or mirror to any filing):
- KSL, 2023 mayoral (`ksl.com/article/50680218/`): Mendenhall **$212,195.48** raised /
  **$249,659.55** spent; Rocky Anderson **$101,779.73** / **$137,608.49**; Michael Valentine
  **$115**. Mendenhall 504 unique donors (~300 under $99). Named: Maverik Inc. $3,700;
  Jody Wilkinson Acura $2,000.
- KSL / Deseret, 2025 council: ~**$400K** across four contested seats; Erika Carlsen and
  Chris Wharton each **>$103,000**. Named: Mendenhall personally, Ben McAdams, Salt Lake
  Board of Realtors, Blaser + Partners, Dustin Buckthal, David Ibarra / Ibarra Brito Centre
  at the **$850 max**.
- Salt Lake Tribune (2023 + 2025 pieces) — **paywalled**, no Wayback snapshot. Snippets
  indicate maxed-out 2023 Mendenhall donors (Bonham, Boyer, Plumb III, Sedky) and Taymour
  Semnani's 2023 D6 bid at **$125,616 incl. $85,438 in self-loans**.
- Axios SLC — exists, Cloudflare-blocked, no snapshot.

These are **secondary reporting, not filings**, and must never be entered as filing data —
they are useful only as a cross-check on totals if the itemized layer is ever recovered.

**Side lead for the rest of the repo (not SLC):** the City Journals' 2019 valley-wide
campaign-finance analysis (`valleyjournals.com/2019/08/19/225828/`, Wayback
`20190914202144`) explicitly examined *"every municipal candidate in the valley (excluding
Salt Lake City proper)"* — a hard negative here, but a genuine positive for the repo's other
SLCo cities: it reports the **Salt Lake Board of Realtors PAC gave $58,000+ across the
valley, averaging $2,252/candidate**, with 10 candidates drawing ≥half their funding from it.

---

## Leads for the coordinator (NOT filed to LEADS.md/TODO.md — that is the coordinator's call)

1. **Structure the 2003 tranche.** 8 born-digital, machine-readable filings, consistent
   layout, ~360 contribution + ~170 expenditure rows. No OCR, no vision needed. This is the
   only itemized SLC municipal CF data the repo can currently hold.
2. **Retry the 3 IA-blocked objects** (`David_Spatafore`, `J_Michael_Clara` in the 2003
   tranche; `Buhler`, `Hughes`, `SaxtonNotice` in limitations). All are confirmed
   `200 application/pdf` in CDX; the Archive is serving a donation interstitial. Persistent
   across ~15 attempts over 40 minutes and across `id_`/`if_`/bare replay forms, cookies and
   Accept headers — try again from a different network or later.
3. **The 503 page is a monitorable data feed.** It renders live balances and will change as
   candidates file. Cheap to re-fetch periodically; also the canary for the app pool's
   return (watch `GetElections` for a non-503).
4. **GRAMA / records request is the real unlock for 2005–2025.** The Recorder's DB is alive
   (it renders the balance table); the old app's files remain on the city's IIS box. The 503
   page itself invites contact — **801-535-7671**, elections@slcgov.com (note: AVAILABILITY
   previously recorded 801-535-6225; both appear). Ask for a DB export or a CSV — the SPA
   bundles `angular2-csv`, so an export path already exists in their tooling.
5. **`slc.gov/attorney/list-of-declared-candidates/`** is what the *state* now points to for
   SLC 2025 — worth harvesting as the candidate spine even without money attached.
6. **DocumentCloud is unexhausted** (anonymous API doesn't phrase-filter). Needs a token.
7. **archive.today is untested, not negative** — captcha-walled to scripting. It renders JS,
   so any human-made capture of the SPA would carry rendered data. Worth one browser check.
8. **Cross-repo lead (other cities, not SLC):** the City Journals 2019 valley-wide analysis
   and the Accountability Project's cleaned bulk mirror of `disclosures.utah.gov`
   (1.38M Utah contribution records 2000–2023) may shortcut CF work for the repo's other
   SLCo cities, which *do* file with the state.

---

# ADDENDUM — 2026-08-14 accessibility re-hunt (round 2)

**Question asked:** has anything that was unreachable on 2026-08-02 become reachable?

**Answer:** yes, but **not from Salt Lake City**. The Internet Archive's interstitial cleared,
recovering all 5 blocked objects. The city's own surfaces are unchanged — measurably so.

## 1. SLC portal — STILL DOWN, and demonstrably untouched

| Probe | 2026-08-02 | 2026-08-14 |
|---|---|---|
| `CampaignFinance_Public/api/CampaignFinanceAPI/GetElections` | 503 | **503** (35,503 B) |
| `ManagementServices/CandidateReporting/{default,candidate_search}.aspx` | 500 | **500** (4,552 B) |
| `CampaignFinance_Candidate/api/…/GetElections` (sibling) | 401 | **401** |
| `CampaignFinance_Public/index.html` (SPA shell) | 200 | **200** |

**The strongest evidence of inaction:** today's 503 body was diffed against the retained
`raw/portal_snapshot/portal_503_under_construction_2026-08-02.html`. The **only** difference
in the entire 35,503-byte document is Cloudflare's rotating `__cf_email__` obfuscation token
(2 lines). The embedded 38-row candidate/office/balance table is **identical**, still stamped
**"Balance as of April 2026"** — the city has not refreshed even its own rendered figures in
the intervening 12 days. No new snapshot was retained: it would be a byte-duplicate.

Unchanged conclusion: server and DB alive, public read surface switched off. GRAMA-shaped.

## 2. IA-interstitial retry — **ALL 5 RECOVERED** (the 2026-08-02 "retry" lead, closed)

CDX re-confirmed each as `200 application/pdf` first; then fetched via `…id_/` replay and
verified by **bytes + magic + rendered content**, not status (GOTCHAS zero-byte rule).

| Object | Bytes | Verified as |
|---|---|---|
| `recorder_2003/David_Spatafore.pdf` | 5,456 | **Real filing** — Mayor, submit 2/18/03; born-digital, itemized |
| `recorder_2003/J_Michael_Clara.pdf` | 5,237 | **Real filing** — Council D2, submit 2/14/2003; born-digital, itemized |
| `recorder_limitations/Buhler.pdf` | 20,902 | Public Notice, 2006-09-22 — **DECLINING** to limit (Mayor) |
| `recorder_limitations/Hughes.pdf` | 20,230 | Public Notice, 2006-07-13 — **DECLINING** to limit (Mayor) |
| `recorder_limitations/SaxtonNotice.pdf` | 33,989 | **Declaration TO limit**, notarized 2004-02-19 (Nancy Saxton, Council) |

All three limitation notices are image-only (no text layer), consistent with that tranche;
verified by rendering page 1, not by filename. **Note a taxonomy correction:** the tranche is
described in `CLAUDE.md`/`AVAILABILITY.md` as declarations "to voluntarily limit" — Buhler and
Hughes are the **opposite instrument** (§2.46.080 notices of a decision to *decline*). Both
directions are public notices under the same section; the folder mixes them.

`recorder_limitations/` is now **15 of 15** — complete.

### Structured-layer effect (additive only)

Re-ran the existing path (`build_finance.py` → `build_index.py`); no code changed.

| | before | after |
|---|---|---|
| filings (`filing_totals.csv` / `index.csv`) | 8 | **10** |
| contributions | 222 | **248** (+26) |
| expenditures | 162 | **176** (+14) |

`git diff`: **44 insertions, 0 deletions** across all four CSVs — no pre-existing row altered.
Both new filings reconcile on **both** sides (Spatafore 8,724.00 / 6,574.00; Clára 956.65 /
745.45). `validate_finance.py` **PASS (0 fails, 0 warns)**; skill `validate_dataset.py` PASS.
Geometry spot-checked through `make_snippet.py` (Spatafore row 58, Jon Greiner 1000.00,
`p1:l29:c34-41` → resolves against the sidecar). Sidecars rendered for both new filings.

**2003 tranche is now 10 of 11.** `Dale_Lambert.pdf` remains the single permanent gap —
re-confirmed by CDX today: the `fin_disc/pdfs/` prefix returns exactly 10 rows, and Lambert
is not among them. Never captured; not recoverable from the Archive.

## 3. Declared-candidates roster — harvested (candidate spine, NOT filings)

`slc.gov/attorney/list-of-declared-candidates/` (what the STATE's municipal tree points to for
SLC 2025) fetched live, plus **two Wayback captures of the 2021 cycle** found by CDX — an
extension beyond the task, since the 2021 roster is a cycle the repo otherwise has no
candidate spine for.

- `raw/declared_candidates/declared_candidates_2025_live_2026-08-14.html` — 2025 cycle,
  13 candidates across D1/D3/D5/D7.
- `…_2021_wayback_20211129171942.html` — 2021 cycle, **final verified** roster (D1/D2/D3/D5/D7
  with ballot order + campaign websites).
- `…_2021_wayback_20210812201021.html` — 2021 mid-filing-window partial (most districts
  "no candidates verified at this point"); retained as the honest partial it is.

Sidecars in `text/declared_candidates/`. **Labelled roster snapshots, never filings — they
carry no money.** Not added to `index.csv` (which is a filing index). The 2025 page also links
per-candidate **conflict-of-interest** PDFs — a different instrument (see channel 3), not
harvested here.

## 4. NEW HOST — `webdme.slcgov.com` (SLC Laserfiche WebLink 11.0.2411.10)

Found via the 2021 roster's "Declaration Packet" links. **Never probed by the 2026-08-02
hunt.** Nine apps; **eight live and anonymously readable**, one retired:

| App | Live | Root folder |
|---|---|---|
| `AgendasMinutes` | 200 | `\Public Documents\Agendas and Minutes` — City Council, RDA, CRA, LBA, Board of Canvassers, Redistricting Committee, Board of Equalization, Mayor Boards |
| `OrdinancesResolutions` | 200 | Council / CRA / LBA / RDA |
| `AdoptedLegislation` | 200 | + **Board of Municipal Canvassers**, Joint Mayor & Council |
| `BoardsCommissions` | 200 | 47 bodies (Planning Commission, Historic Landmark, Housing Authority…) |
| `PlanningBoardsCommissions` | 200 | PC, HLC, Appeals, Administrative Hearing |
| `Planning` | 200 | General Plans, Zoning Maps, Administrative Determinations |
| `GeneralBusiness` | 200 | Executive Orders, Emergency Proclamations |
| `BldgPermitHistory` | 200 | `\CAN\Building Permits History` |
| **`CityElections`** | **404** | **RETIRED** — the app the city's own 2021 roster linked to |

API recovered from `app/dist/browse/main.js` (same method as the CF SPA): `GetRootFolderId`,
`GetFolderListing2`, `GetFolderListingIds`, `GetSearchListing`, `GetBasicDocumentInfo`, etc.
Anonymous **search rights = true**; `{LF:Basic~="campaign finance", option="DFANLT"}` returns
**397 hits** — all Council *Meeting Materials* packets (legislative discussion of §2.46), i.e.
loose word-match noise, **not filings**. Rights are enforced per-entry, so each app sees only
its own subtree (`\Public Documents` itself lists 1 child from an `AgendasMinutes` session).

**Earned negative for campaign finance:** all eight live app roots enumerated; **no
campaign-finance and no elections-filings subtree is publicly exposed anywhere on the host.**
The only election-adjacent folders are *Board of Canvassers* and *Board of Municipal
Canvassers* — vote certification, not money. The retired `CityElections` subtree is
unreachable: its 2021 DocView ids (e.g. 3846445) return `Entry not found [9001]` via `/WebLink/`
and `id: 0, name: null` via a live app's `GetBasicDocumentInfo`. Wayback holds 30 CityElections
captures — DocView **shells only, zero PDFs** (host-wide CDX sweep: 5,145 captures, no PDF).

**This is a significant lead for the repo beyond campaign finance** — filed to `LEADS.md`.

## 5. Channels NOT run this round (scope call, not negatives)

The owner narrowed scope mid-session to "is SLC's own portal fixed". Left untouched, still
**unexhausted rather than negative**:

- **archive.today** — retried once in a real browser (c11 WKWebView): still the captcha
  interstitial, and the challenge widget does not render in WKWebView, so it cannot be
  solved there. Mirrors (`archive.is/li/md/vn`) untried.
- **DocumentCloud** — **status changed since 2026-08-02**: the anonymous API
  (`api.www.documentcloud.org/api/documents/search/`) is now **Cloudflare-challenged to
  scripts** (returns "Attention Required!" HTML, not JSON). Still needs a token or a browser.
- **GRAMA letter** — not drafted this round.

## Probe log (this round)

| # | Target | Method | Result |
|---|---|---|---|
| 1 | `GetElections` | GET, browser UA | **503**, byte-identical body |
| 2 | `CandidateReporting/*.aspx` | GET ×2 | **500** |
| 3 | Candidate-app API | GET | **401** (unchanged) |
| 4 | SPA shell | GET | 200 |
| 5 | CDX `fin_disc/pdfs/` | CDX | 10 rows — Lambert absent, permanent |
| 6 | CDX `pdfs/limitations/` | CDX | 15 rows — all present |
| 7 | 5 blocked objects | `id_` replay | **ALL 5 RECOVERED**, verified by bytes + render |
| 8 | `slc.gov` roster (live + CDX) | GET | 2025 live + **2021 ×2 in Wayback** |
| 9 | `webdme.slcgov.com` | app sweep + WebLink API + host CDX | 8 live apps, `CityElections` 404; **no CF subtree** |
| 10 | archive.today | c11 browser | captcha, unsolvable in WKWebView |
| 11 | DocumentCloud API | GET | **now Cloudflare-blocked** (regression vs 08-02) |
